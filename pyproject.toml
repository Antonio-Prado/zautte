[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
# E402: module-level import not at top of file
# Ignorato perché i moduli usano sys.path.insert() prima degli import interni.
ignore = ["E402"]
